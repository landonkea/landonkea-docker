# ── Git ignore rules for multi-container-app ─────────────────────

app/node_modules   # npm dependencies installed by the app (not committed)
.vscode            # VS Code editor settings (per-machine, not shared)
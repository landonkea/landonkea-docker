# ── Security: block npm install scripts ──────────────────────────
# "ignore-scripts=true" tells npm to skip running lifecycle hooks
# (preinstall, postinstall, etc.) when installing packages.
# Malicious packages can use these hooks to run arbitrary code on
# your machine.  Setting this flag prevents that attack vector.
ignore-scripts=true
